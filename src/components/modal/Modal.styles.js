import styled from 'styled-components';

const Timer = styled.h1`
    display: flex;
    height: 4rem;
    text-align: center;
    align-items: center;
    justify-content: center;
`;

export default Timer;
